// Server-side view of the shared catalogue. Single source of truth lives in
// public/assets/pricing.js so the booking form and the API always agree.
module.exports = require('../public/assets/pricing.js');
